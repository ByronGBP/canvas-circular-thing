const OFFSET100 = Math.max(window.innerHeight, window.innerWidth) / 100;
const OFFSET500 = Math.max(window.innerHeight, window.innerWidth) / 500;
const OFFSET_X = Math.max(window.innerHeight, window.innerWidth) / 250;
const OFFSET_O = window.innerWidth / 25;
const MAX = Math.max(window.innerHeight, window.innerWidth);
const TWO_PI = Math.PI * 2;
const RADIUS = 10;
// const TEXT_WIN = [87, 97, 110, 110, 97, 32, 116, 97, 107, 101, 32, 115, 111, 109, 101, 32, 119, 105, 110, 101, 63, 32, 58, 41];
const TEXT_WIN = 'FCK U! :)';
const OFFSET_GOOD = {
  r: OFFSET500,
  c: OFFSET100
};

const OFFSET_CRAZY = {
  r: OFFSET500,
  c: OFFSET500
};
